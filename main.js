const git = require('isomorphic-git');
const http = require('isomorphic-git/http/web');
const fs = require('@isomorphic-git/lightning-fs');
const XLSX = require("xlsx");

module.exports = { git,http,fs,XLSX };
